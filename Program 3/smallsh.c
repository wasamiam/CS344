#include <stdlib.h>
#include <stdio.h>

int main(int argc, char **argv){

  // Commandline loop
  do {
    // Print prompt
    printf(": ");
    fflush();
    // Read line
    char* line = NULL;
    size_t n = 2048;
    getdelim(&line, &n, ' ', stdin)
  } while();


  // Exit
}
